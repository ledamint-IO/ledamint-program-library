{
  "name": "@leda-mint-io/lpl-ledamint",
  "version": "0.0.1",
  "description": "LPL JavaScript API.",
  "main": "dist/src/mpl-metaplex.js",
  "types": "dist/src/mpl-metaplex.d.ts",
  "scripts": {
    "check:publish-ready": "yarn build && yarn test",
    "preversion": "yarn check:publish-ready",
    "postversion": "git add package.json && git commit -m \"chore: update $npm_package_name to v$npm_package_version\" && git tag $npm_package_name@$npm_package_version",
    "prepublish": "yarn check:publish-ready",
    "postpublish": "git push origin && git push origin --tags",
    "build:docs": "typedoc",
    "build": "rimraf dist && tsc -p tsconfig.json",
    "test": "echo mpl-metaplex tests coming up ...",
    "lint": "eslint \"{src,test}/**/*.ts\" --format stylish",
    "fix:lint": "yarn lint --fix",
    "prettier": "prettier \"{src,test}/**/*.ts\" --check",
    "fix:prettier": "prettier --write src/",
    "fix": "yarn fix:lint && yarn fix:prettier"
  },
  "files": [
    "/dist"
  ],
  "publishConfig": {
    "access": "public",
    "registry": "https://registry.npmjs.org"
  },
  "keywords": [
    "nft",
    "metaplex",
    "ledamint",
    "safecoin",
    "blockchain"
  ],
  "homepage": "https://ledamint.io",
  "repository": "https://github.com/metaplex-foundation/metaplex-program-library.git",
  "author": "Metaplex Maintainers <contact@metaplex.com>",
  "license": "MIT",
  "private": false,
  "dependencies": {
    "@leda-mint-io/lpl-auction": "^0.0.2",
    "@leda-mint-io/lpl-core": "^0.0.51",
    "@leda-mint-io/lpl-token-metadata": "^1.2.52",
    "@leda-mint-io/lpl-token-vault": "^0.0.191",
    "@safecoin/safe-token": "^0.1.7-b",
    "@safecoin/web3.js": "^1.29.4"
  },
  "devDependencies": {
    "eslint": "^8.3.0",
    "rimraf": "^3.0.2"
  }
}
